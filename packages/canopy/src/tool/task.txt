Launch a sub-agent for complex multi-step tasks.

Use when: complex research, multi-file changes, any task needing subtask planning.

NOT for: reading single files (use Read), grep/glob searches (use Grep/Glob), reading 2-3 known files (use Read).

Each invocation is fresh context. Provide detailed instructions and specify expected output format. Launch multiple agents in parallel when possible. Outputs are trusted — verify critical results yourself. Do not duplicate delegated work.