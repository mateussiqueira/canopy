Create/manage task list for the current session. Use for multi-step work (3+ steps), planning, or when user provides numbered tasks.

Only one `in_progress` at a time. Mark `completed` only after actual verification. Update in real-time — don't batch.

If blocked, keep `in_progress` and add follow-up. Items must be specific and actionable. When in doubt, use it.